# Migration `20200609143758-init`

This migration has been generated by quino0627 at 6/9/2020, 2:37:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `qufit-web`.`Exercise` (
`caution` varchar(191) NOT NULL  ,`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`deletedAt` datetime(3)   ,`description` varchar(191) NOT NULL  ,`id` int NOT NULL  ,`machineId` int NOT NULL ,`name` varchar(191) NOT NULL  ,`updated` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `qufit-web`.`Machine` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`deletedAt` datetime(3)   ,`id` int NOT NULL  ,`name` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `qufit-web`.`Class` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`deletedAt` datetime(3)   ,`id` int NOT NULL  ,`name` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `qufit-web`.`ClassesOnMachines` (
`classId` int NOT NULL  ,`machineId` int NOT NULL ,
    PRIMARY KEY (`machineId`,`classId`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `qufit-web`.`Post` (
`createdAt` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ,`deletedAt` datetime(3)   ,`id` int NOT NULL  ,`title` varchar(191) NOT NULL  ,`updatedAt` datetime(3) NOT NULL  ,
    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `qufit-web`.`Exercise` ADD FOREIGN KEY (`machineId`) REFERENCES `qufit-web`.`Machine`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `qufit-web`.`ClassesOnMachines` ADD FOREIGN KEY (`machineId`) REFERENCES `qufit-web`.`Machine`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `qufit-web`.`ClassesOnMachines` ADD FOREIGN KEY (`machineId`) REFERENCES `qufit-web`.`Class`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200609143758-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,65 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Exercise {
+  id          Int       @default(uuid()) @id
+  name        String
+  caution     String
+  // 이미지 혹은 영상
+  description String
+  usedMachine Machine   @relation(fields: [machineId], references: [id])
+  machineId   Int
+  createdAt   DateTime  @default(now())
+  updated     DateTime  @updatedAt
+  deletedAt   DateTime?
+}
+
+// Machine에 맨몸 포함
+model Machine {
+  id        Int                 @default(uuid()) @id
+  name      String
+  createdAt DateTime            @default(now())
+  updatedAt DateTime            @updatedAt
+  deletedAt DateTime?
+  exercises Exercise[]
+  classes   ClassesOnMachines[]
+}
+
+model Class {
+  id        Int                 @default(uuid()) @id
+  name      String
+  createdAt DateTime            @default(now())
+  updatedAt DateTime            @updatedAt
+  deletedAt DateTime?
+  machines  ClassesOnMachines[]
+}
+
+model ClassesOnMachines {
+  machine   Machine @relation(fields: [machineId], references: [id])
+  machineId Int
+  class     Class   @relation(fields: [machineId], references: [id])
+  classId   Int
+  @@id([machineId, classId])
+}
+
+model Post {
+  id        Int       @default(uuid()) @id
+  title     String
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  deletedAt DateTime?
+}
+
+enum Role {
+  USER
+  ADMIN
+}
```


